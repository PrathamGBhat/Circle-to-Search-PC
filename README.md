pip install -r requirements.txt

1. Run control.pyw
2. Start process
3. Can close control.pyw as its a separate process from listener.py
4. listener.py runs as the background process
5. Select any text and press ctrl+space
6. Open log.txt at the project root to see events, captured text, responses, and errors